## Packages
zustand | Client-side state management for the shopping cart

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The UI uses a minimalist fashion aesthetic.
All product image URLs from the API should ideally be high-quality portraits, but fallbacks are handled via CSS object-cover.
